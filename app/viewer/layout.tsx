export const metadata = {
  title: 'Elite Aligner - 3D Viewer',
  description: 'عرض نماذج تقويم الأسنان ثلاثية الأبعاد',
};

export default function ViewerLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}